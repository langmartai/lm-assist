# v0.2.0 Release + Docs Scope-Rebase Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (inline). Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish the first npm release since 0.1.70 as v0.2.0, with README/docs reorganized around the product's current scope (fleet control plane + MCP connector + connectors + automation), shipped as git tag + GitHub Release tgz + npm publish of the same artifact.

**Architecture:** Docs-first, then version bump, then one packed artifact verified once and released to both channels. No core/web code changes — main (`ea5302c`) is the release content and is already the code the fleet runs.

**Tech Stack:** npm workspaces, tsc, Next.js 16 (`next build`, Node ≥20), `npm pack`, `gh release`.

**Spec:** the operator's request (complete new release: npm package + README + doc reorganization + scope rebase for the new feature scope) + `docs/build-pack-install-upgrade.md` (authoritative release process).

## Global Constraints

- Node ≥ 20.9 (v20.19.6 on PATH). Run npm from the repo ROOT only.
- chokidar stays `^3.6.0` in both `package.json` and `core/package.json`; verify it resolves to 3.6.x before packing.
- Three version fields move together to `0.2.0`: `package.json`, `.claude-plugin/plugin.json`, the **lm-assist entry** in `.claude-plugin/marketplace.json` (other plugin entries in that file belong to other projects — do not touch).
- PUBLIC repo + npm: no private repo names/owners/author emails, no internal IPs/hostnames, no credentials in any new/edited file or in the packed tarball. Grep before commit and before publish.
- Do not touch running services (prod :3100/:3848, dev :3200/:3948). No installs of the new package on this host. `env -u API_PORT` for any core.sh use.
- Do not disturb the background PR-#18 review agent's worktree; do not run the full test suite concurrently with it.

---

### Task 1: CHANGELOG — consolidated 0.2.0 entry

**Files:** Modify: `CHANGELOG.md`

- [ ] Add `## [0.2.0] - 2026-09-01` at top: headline (first npm publish since 0.1.70; ~1690 commits; scope), grouped highlights (MCP connector & tools · fleet/hub · connectors · missions/automation · memory/search · voice · UI/panes · bundled plugins · packaging/upgrade fixes incl. chokidar pin), and an explicit note that every section below down to [0.1.70] — including blocks previously labeled `[Unreleased]` — ships in this npm release.
- [ ] Relabel the four `## [Unreleased]` headings to `## [Unreleased → 0.2.0]` so nothing still claims to be unreleased.

### Task 2: README — scope rebase

**Files:** Modify: `README.md`

- [ ] Rewrite around the current scope: self-hosted control plane for Claude Code — Observe / Control / Fleet / Connect (MCP) / Automate / Remember / Extend.
- [ ] Use measured numbers only: 863 REST route handlers (“860+”), 285 registered MCP tools (“280+”), 15 insight views, connector list from code (Gmail, LinkedIn, WhatsApp, mobile, GitHub, VM, container, desktop, voice), bundled plugin system.
- [ ] Keep: install paths (npm / marketplace / source), services table, data-sources table, AGPL license, Discord badge, related-plugins, Medium links. Point “docs” at `docs/README.md`.
- [ ] Requirements: Node ≥ 20.9 (engines say 20.9; old README said 18 — fix).

### Task 3: docs/README.md — the reorganized index

**Files:** Create: `docs/README.md`

- [ ] Categorized index of the existing ~45 docs (Getting started / Architecture & internals / API reference / Feature guides / Connectors / MCP & plugins / Operations & deploy / Engineering notes: perf, reviews, plans, specs). One line per doc. **No file moves** — CLAUDE.md topic triggers, fleet memory, and external deep links depend on current paths; the reorganization is a curated navigation layer.

### Task 4: NPM_PACKAGE.md refresh + CLAUDE.md intro line

**Files:** Modify: `NPM_PACKAGE.md`, `CLAUDE.md`

- [ ] NPM_PACKAGE.md: Node ≥ 20.9, current CLI commands, current package contents (MCP server, hub client, connectors), drop stale claims; keep screenshots.
- [ ] CLAUDE.md: update only the opening description sentence to the new scope (index structure untouched).

### Task 5: Version bump + files list

**Files:** Modify: `package.json`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`

- [ ] All three lm-assist version fields → `0.2.0`.
- [ ] `CHANGELOG.md` deliberately stays OUT of package.json `files`: historical entries carry
  pre-existing references that must not gain a new publication surface; the README links the
  changelog on GitHub instead.

### Task 6: Build + pack + verify the artifact

- [ ] `npm install --ignore-scripts` (root; worktree has no node_modules yet).
- [ ] Verify `require('chokidar/package.json').version` → 3.6.x; ensure no nested `core/node_modules` shadow appears.
- [ ] `npm pack` (prepare builds core + web) → `lm-assist-0.2.0.tgz`.
- [ ] Verify tarball: contains `core/dist`, `core/data/mcp-plugins` (bundled plugin + bundled.json), `core/scripts/upgrade.js`, `ccr/`, `web/.next`, `bin/`, `.claude-plugin`; chokidar pin `^3.6.0` in both packaged package.json files; size sane (~28 MB); `node --check` on bin + cli entry from the extracted copy.
- [ ] Leak grep over the extracted tarball and over the git diff: private repo names/owners, operator email, internal IPs → must be zero hits in anything NEW (pre-existing published refs in docs/ do not ship — `files` excludes docs/).

### Task 7: Commit + push + tag + GitHub Release

- [ ] Commit docs + bump (+ this plan file); push `HEAD:main` to origin.
- [ ] `git tag v0.2.0` + push tag; `gh release create v0.2.0 lm-assist-0.2.0.tgz` with release notes from the CHANGELOG 0.2.0 section (installer’s preferred source is exactly `releases/download/v0.2.0/lm-assist-0.2.0.tgz`).

### Task 8: npm publish (same artifact)

- [ ] `npm publish lm-assist-0.2.0.tgz` — publishes the byte-identical verified tarball, no rebuild. Known risk: `npm whoami` currently 401 (expired token) → if it fails, report the exact operator action (`npm login`, then the same publish command) and stop there.
- [ ] On success: `npm view lm-assist version` → 0.2.0; then a follow-up commit updating `docs/build-pack-install-upgrade.md`'s “npm latest = 0.1.70 / don’t install from npm” statements to the new truth.

### Explicitly out of scope
- Fleet node upgrades to 0.2.0 (offer as follow-up; §6 of the build doc describes it).
- Merging PR #18 (background agent reviewing; release content is main as of `ea5302c`).
- Scrubbing pre-existing private-name references in committed docs/ history (separate, unauthorized job; they do not ship in the tarball).
